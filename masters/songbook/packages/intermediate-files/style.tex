\usepackage{graphicx}

\usepackage[
  bottom=0.8cm,
  footskip=0cm,
  headsep=0cm,
  includeheadfoot,
  inner=1cm,
  landscape,
  outer=0.5cm,
  %showframe,
  top=0.5cm,
  nomarginpar,
]{geometry}

% Innenraum der einzelnen Kacheln
% 19.2 auf 14.1 = 1,361702128

% 5 / 7 = 1,4
% √2 = 1,414213562
% Din A4 297 / 210 = 1,414285714

%-----------------------------------------------------------------------
%
%-----------------------------------------------------------------------

\RequirePackage{polyglossia}
\setdefaultlanguage{german}

%-----------------------------------------------------------------------
%
%-----------------------------------------------------------------------

\usepackage{fontspec}
\setmainfont{Alegreya}[
  Ligatures = TeX,
  Numbers = OldStyle,
  UprightFeatures = { SmallCapsFont={ *SC } },
  BoldFeatures = { SmallCapsFont={ *SC-Bold } },
  ItalicFeatures = { SmallCapsFont={ *SC-Italic  } },
  BoldItalicFeatures = { SmallCapsFont={ *SC-BoldItalic } },
]
\setsansfont{AlegreyaSans}[
  Ligatures = TeX,
  Numbers = OldStyle,
  BoldFont = *-ExtraBold,
  ItalicFont = *-BoldItalic,
  BoldItalicFont = *-ExtraBoldItalic,
  UprightFont = *-Bold,
  UprightFeatures = { SmallCapsFont={ *SC-Bold } },
  BoldFeatures = { SmallCapsFont={ *SC-ExtraBold } },
  ItalicFeatures = { SmallCapsFont={ *SC-BoldItalic  } },
  BoldItalicFeatures = { SmallCapsFont={ *SC-ExtraBoldItalic } },
]

%-----------------------------------------------------------------------
% multicol
%-----------------------------------------------------------------------

\usepackage{multicol}
\setlength{\columnseprule}{0pt}

%-----------------------------------------------------------------------
% fancyhdr
%-----------------------------------------------------------------------

\def\tmpABC{{\sffamily\bfseries\leftmark}}
\usepackage{fancyhdr}
\fancyhead{} % clear all header fields
% Outer
% \left: A
% \right: Auf der Mauer, auf der Lauer
\fancyhead[LE]{
  \tmpABC
  \hspace{1cm}
  \rightmark
}
\fancyhead[RO]{
  \rightmark
  \hspace{1cm}
  \tmpABC
}
% Inner
\fancyhead[LO,RE]{}
\fancyfoot{}
\fancyfoot[LE,RO]{\raisebox{10cm}{\thepage}}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\pagestyle{fancy}
\renewcommand{\sectionmark}[1]{\markboth{#1}{}}
\renewcommand{\subsectionmark}[1]{}

%-----------------------------------------------------------------------
%
%-----------------------------------------------------------------------

\usepackage{titlesec}
\titlespacing{\section}{0pt}{0pt}{0pt}
%\titleformat{command}[shape]{format}{label}{sep}{before-code}[after-code]
\titleformat{\subsection}[runin]{\bfseries}{}{0pt}{}

%-----------------------------------------------------------------------
% Custom macros
%-----------------------------------------------------------------------

%%
% \tmpcolumnbreak
% To be more flexible in the future. We can extend this minimal
% macro.
%%
\newcommand{\tmpcolumnbreak}{\columnbreak}

%%
% \tmptitle
%%
\newcommand{\tmpmetadata}[4]{%
  \subsection{#1} % title
  { %
    \footnotesize
    \textit{#2} % subtitle
    \textsc{#3} % composer
    \textit{\textsc{#4}} % lyricist
  }
}

%%
% \tmpimage
%%
\newcommand{\tmpimage}[1]{%
  \begin{center}
  \includegraphics[scale=0.95]{#1}%
  \end{center}
}

%%
% \tmpchapter
%%
\newcommand{\tmpchapter}[1]{%
\end{multicols*}

\cleardoublepage

\section{#1}%

\begin{multicols*}{2}
}

%%
% \tmpplaceholder
%%
\newcommand{\tmpplaceholder}{%
  \begin{center}
  \strut
  \vfill
  This column intentionally left blank.
  \vfill
  \strut
  \end{center}
}

\setcounter{secnumdepth}{-1}