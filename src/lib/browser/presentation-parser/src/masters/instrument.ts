import { Master } from '../master'

export class InstrumentMaster extends Master {
  name = 'instrument'

  displayName = 'Instrument'

  iconSpec = {
    name: 'instrument',
    color: 'yellow'
  }
}
