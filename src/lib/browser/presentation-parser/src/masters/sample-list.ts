import { Master } from '../master'

export class SampleListMaster extends Master {
  name = 'sampleList'

  displayName = 'Audio-Ausschnitte'

  iconSpec = {
    name: 'music',
    color: 'red'
  }
}
