import { Master } from '../master'

export class ScoreSampleMaster extends Master {
  name = 'scoreSample'

  displayName = 'Notenbeispiel'

  iconSpec = {
    name: 'file-audio',
    color: 'black'
  }
}
