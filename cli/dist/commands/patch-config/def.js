import { validateDefintion } from '../../main';
export default validateDefintion({
    command: 'patch-config',
    alias: 'pc',
    description: 'Patch /etc/baldr.json with the media categories, the sass variables.'
});
