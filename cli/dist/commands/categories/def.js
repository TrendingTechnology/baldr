import { validateDefintion } from '../../main.js';
export default validateDefintion({
    command: 'categories',
    alias: 'ca',
    description: 'Patch /etc/baldr.json with the media categories.'
});
